apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "evebot.fullname" . }}-test-connection"
  labels:
    {{- include "evebot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "evebot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
