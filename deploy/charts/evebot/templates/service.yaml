apiVersion: v1
kind: Service
metadata:
  name: {{ include "evebot.fullname" . }}
  labels:
    {{- include "evebot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "evebot.selectorLabels" . | nindent 4 }}
